{
  "tasks": {
    "install": "cd client && npm install",
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build-tiles": "deno run --env-file --allow-read --allow-write --allow-env --allow-run=magick,convert,identify scripts/build_tiles.ts",
    "serve": "deno run --env-file --allow-net --allow-read --allow-env --allow-run=magick,convert server.ts",
    "start": "deno task build && deno task serve",
    "run": "deno run --env-file --allow-read --allow-write --allow-env --allow-net=imagemagick.org --allow-run=deno scripts/run.ts"
  },
  "fmt": {
    "include": ["server.ts"]
  }
}
